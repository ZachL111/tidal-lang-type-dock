# Tidal Lang Type Dock Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | IR pressure | 198 | ship |
| stress | lowering drift | 210 | ship |
| edge | stack depth | 194 | ship |
| recovery | diagnostic reach | 210 | ship |
| stale | IR pressure | 201 | ship |

Start with `stress` and `edge`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `lowering drift` against `stack depth`, not the raw score alone.
